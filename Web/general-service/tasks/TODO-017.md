# TODO-017: 인증·서버 측 인가 도입

## 상태

`TODO`

## 현재 분석

현재 누구나 API를 호출할 수 있고, 요청 본문의 `taskOwner`·`outcomeOwner` 문자열이 행위자 검증에 사용된다. 이는 신원 인증이 아니므로 실제 권한 통제가 아니다.

## 목표

인증된 사용자 정체성을 기반으로 Task와 Outcome의 읽기·수정·제출·검토 권한을 서버에서 강제한다.

## 선행 작업

TODO-006, TODO-007, 사람의 인증 방식·역할 정책 승인

## 완료 조건

- [ ] SSO, 세션 또는 토큰 방식과 역할·소유자 권한 모델을 ADR로 결정한다.
- [ ] 인증 미들웨어가 신원 정보를 애플리케이션 계층에 전달한다.
- [ ] API 본문의 담당자 값 대신 인증된 주체로 제출·검토를 인가한다.
- [ ] 미인증(401)·미인가(403)·소유자 불일치 경로를 자동 테스트한다.

## 영향 범위

`src/Interface/Http/TaskRoutes.hs`, 새 인증 모듈, `src/Application/TaskService.hs`, Elm 로그인 흐름, `docs/SECURITY.md`
