# UX 명세

초기 제품은 HTTP API이다.

## 기본 흐름

Todo 생성 -> 목록에서 확인 -> 완료 -> 선택적으로 삭제.

## 검증

다음 제목은 유효하지 않다: `""`, `" "`, `"\n"`, `"\t"`.

## 완료

클라이언트는 완료 요청을 재시도할 수 있다. 완료 요청을 반복해도 충돌이 아닌 성공을 반환해야 한다.

## 찾을 수 없음

존재하지 않는 Todo의 조회, 완료 또는 삭제는 제어된 찾을 수 없음 응답을 반환한다.
