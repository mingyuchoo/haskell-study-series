# 보안 명세

버전 1에는 인증이 없으므로 외부 접근 제어 계층 없이는 안전한 다중 사용자 공개 서비스로 취급해서는 안 된다.

- 모든 외부 입력을 검증한다.
- 형식이 잘못된 UUID는 리포지터리에 접근하기 전에 거부한다.
- SQL 값은 반드시 매개변수화한다.
- `DATABASE_URL` 또는 기타 비밀 정보를 절대 커밋하지 않는다.
- 자격 증명이 포함된 전체 데이터베이스 URL을 로그에 남기지 않는다.
- 원시 데이터베이스 예외를 클라이언트에 반환하지 않는다.
- 구체적인 필요가 있을 때만 의존성을 추가한다.
