name:                app-using-stack-on-nix
version:             0.1.0.0
github:              "githubuser/app-using-stack-on-nix"
license:             BSD3
author:              "Author name here"
maintainer:          "example@example.com"
copyright:           "2021 Author name here"

extra-source-files:
- README.md
- ChangeLog.md

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            Web

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on GitHub at <https://github.com/githubuser/app-using-stack-on-nix#readme>

dependencies:
- base >= 4.7 && < 5
- containers
- flow
- parallel

library:
  source-dirs: src

executables:
  app-using-stack-on-nix-exe:
    main:                Main.hs
    source-dirs:         app
    ghc-options:
    - -dynamic
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N4
    dependencies:
    - app-using-stack-on-nix

tests:
  app-using-stack-on-nix-test:
    main:                Spec.hs
    source-dirs:         test
    ghc-options:
    - -dynamic
    - -threaded
    - -rtsopts
    - -with-rtsopts=-N4
    dependencies:
    - app-using-stack-on-nix
